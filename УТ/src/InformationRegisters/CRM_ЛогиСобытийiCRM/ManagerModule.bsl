#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ПрограммныйИнтерфейс

// Процедура выполняет запись события в регистр.
//
// Параметры:
//  ОписаниеСобытия	 - Строка	 - Описание события.
//  ТипСобытия		 - ПеречислениеСсылка.CRM_ТипыСобытийЛогаiCRM, Неопределено	 - Тип события.
//  УзелОбмена		 - ПланОбменаСсылка.CRM_СинхронизацияСМобильнымПриложениемНовый - Узел плана обмена.
//
Процедура ЗаписатьСобытие(ОписаниеСобытия = "", ТипСобытия = Неопределено, УзелОбмена) Экспорт
	
	Если НЕ ЗначениеЗаполнено(УзелОбмена) Тогда
		Возврат;
	КонецЕсли;
	
	Запись = РегистрыСведений.CRM_ЛогиСобытийiCRM.СоздатьМенеджерЗаписи();
	Запись.ИдентификаторСобытия = Новый УникальныйИдентификатор;
	Запись.ТипСобытия = ТипСобытия;
	Запись.ОписаниеСобытия = ОписаниеСобытия;
	Запись.УзелОбмена = УзелОбмена;
	Запись.Записать();
	
КонецПроцедуры

#КонецОбласти

#КонецЕсли