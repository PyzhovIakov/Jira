
#Область ОбработчикиСобытий

&НаСервере
Функция ЭтоРазделенныйРежимРаботы()

	Возврат Константы.ИспользоватьРазделениеПоОбластямДанных.Получить();

КонецФункции 

&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	Если ЭтоРазделенныйРежимРаботы() Тогда
		ОткрытьФорму("Обработка.CRM_Приложения.Форма.ФормаФреш");
	Иначе
		ОткрытьФорму("Обработка.CRM_Приложения.Форма");
	КонецЕсли;
КонецПроцедуры

#КонецОбласти
