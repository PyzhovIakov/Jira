
#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура СоздатьРассылку(Команда)
	
	ТекДанные = Элементы.Список.ТекущиеДанные;
	Если ТекДанные = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	CRM_ЦентрМониторингаКлиент.НачатьЗамерОперацииБизнесСтатистики(
		"CRM_Статистика.Маркетинг.РассылкаЭлектронныхПисем.ДлительностьСценариев.ВремяОткрытияФормы", Истина);
	
	ФормаРассылки = ПолучитьФорму("Документ.CRM_РассылкаЭлектронныхПисем.ФормаОбъекта");
	ДокОбъект = ФормаРассылки.Объект; 
	CRM_МетодыФормДокументовСервер.ЗаполнитьСозданныйДокументРассылки(ДокОбъект, ТекДанные.Ссылка);
	КопироватьДанныеФормы(ДокОбъект, ФормаРассылки.Объект);
	ФормаРассылки.Открыть();
	
КонецПроцедуры

#КонецОбласти
