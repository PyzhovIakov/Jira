//	СообщениеИзменяемое - Неопределено - может быть установлено для модификации
//	Результат - Строка - если будет установлено значение - установленное значение будет приведено к строке и передано собеседнику.
//		По-умолчанию заполнен текстом блока


// нет вывода текста
Результат = Неопределено;

// вывод текста блока
Результат = Результат;


// вывод текста
Результат = "Превед, Медвед!";


// эхо с автоудалением
Текст = ч.ПредставлениеЗначения(ч.Данные);
Если не ПустаяСтрока(Текст) тогда
	ч.ОтправитьТекст(СтрШаблон("<i>%1</i>", Текст), Новый Структура("Автоудаление", 15));
КонецЕсли;

// ответ на сообщение и автоудаление сообщения
ч.Параметры.Вставить("ОтветНа", ч.СообщениеПолученное);
ч.Параметры.Вставить("Автоудаление", 5);

// показать клавиатуру ответа
Клавиатура = Новый СписокЗначений();
Список = Новый СписокЗначений();
Список.Добавить("Круг"); Список.Добавить("Квадрат"); 
Клавиатура.Добавить(Список);
Клавиатура.Добавить("В виде котенка");
ч.ОтправитьТекст("Какая форма нужна?", Новый Структура("КлавиатураОтвета", Клавиатура));
